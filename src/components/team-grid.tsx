import { barbers } from "@/lib/barbershop-data";
export function TeamGrid() { return <div className="grid gap-px bg-surface-deep-border md:grid-cols-3">{barbers.map((barber, index) => <article key={barber.name} className="relative bg-surface-deep p-7 transition-[transform,box-shadow] duration-300 hover:z-10 hover:-translate-y-1 hover:shadow-xl sm:p-9"><span className="font-display text-5xl text-primary/45" aria-hidden="true">0{index + 1}</span><p className="mt-8 text-xs font-bold uppercase tracking-[0.14em] text-primary">{barber.role}</p><h3 className="mt-2 font-display text-3xl font-semibold text-surface-deep-foreground">{barber.name}</h3><p className="mt-3 text-sm leading-6 text-surface-deep-muted">{barber.bio}</p></article>)}</div>; }