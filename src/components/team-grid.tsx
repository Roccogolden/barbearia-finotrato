import { barbers } from "@/lib/barbershop-data";
export function TeamGrid() { return <div className="grid gap-6 md:grid-cols-3">{barbers.map((barber) => <article key={barber.name} className="group"><div className="aspect-[4/5] overflow-hidden bg-muted"><img src={barber.image} alt={`Retrato de ${barber.name}`} width={800} height={1008} loading="lazy" className="h-full w-full object-cover grayscale-[15%] transition duration-700 group-hover:scale-[1.03] group-hover:grayscale-0" /></div><p className="mt-5 text-xs font-bold uppercase tracking-[0.14em] text-primary">{barber.role}</p><h3 className="mt-2 font-display text-3xl font-semibold">{barber.name}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{barber.bio}</p></article>)}</div>; }